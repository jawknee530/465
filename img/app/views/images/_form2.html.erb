<%= form_for(@image) do |f| %>

      <div class="field">
        <%= f.label :private %><br>
        <%= f.number_field :private %>
      </div>
      <div class="actions">
        <%= f.submit %>
      </div>
    <% end %>
