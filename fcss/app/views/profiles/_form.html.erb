<%= form_for(@profile) do |f| %>
  <% if @profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
      <% @profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address%>
  </div>
  <p>Where will you be X-mas day?<p>
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location%>
  </div>
  <p>Should we be able to choose who not to get? yes/no</p>
  <div class="field">
    <%= f.label :exclude%><br>
    <%= f.text_field :exclude%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
